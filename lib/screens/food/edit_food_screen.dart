import 'package:flutter/material.dart';

class EditFoodScreen extends StatelessWidget {
  const EditFoodScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Edit Food Screen'),
      ),
    );
  }
}
